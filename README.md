# Задача

Реализовать код в строках, отмеченных FIXME в файле `src/Messages/Messages.jsx` так, чтобы работало сохранение сообщений

Для запуска проекта:
1. `npm i && npm run dev` в одном терминале
2. `node server.mjs` в другом терминале

В качестве результата (для зачёта) отправить ТОЛЬКО содержимое файла `src/Messages/Messages.jsx` (другие файлы менять и отправлять не нужно) на coursar@ya.ru с темой: SLIT-1001, ФИО

Дедлайн приёма практики: Пн, 23.11.2025, 10:00 MSK
